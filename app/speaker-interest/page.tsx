import type { Metadata } from 'next';
import Header from '../../components/Header';import Footer from '../../components/Footer';import SpeakerInterestForm from '../../components/SpeakerInterestForm';
export const metadata: Metadata = { title: 'Speaker Interest', description: 'Express interest in contributing to a Banking CEE event as a speaker, panelist or Advisory Board member.' };
export default async function Page({searchParams}:{searchParams:Promise<{event?:string}>}){const p=await searchParams;return <main className="partnership-enquiry-page"><section className="partner-hero partnership-form-hero"><Header/><div className="shell partner-hero-content"><p className="eyebrow">Contribute to Banking CEE</p><h1>Share Your Experience With the CEE Banking Community.</h1><p>Express your interest in contributing to an upcoming Banking CEE event as a speaker, panelist or Advisory Board member.</p></div></section><section className="section-light"><div className="shell partnership-form-shell"><SpeakerInterestForm initialEvent={p.event||''}/></div></section><Footer/></main>}
