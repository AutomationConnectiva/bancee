import {NextResponse} from 'next/server';
export const runtime='nodejs';
export async function POST(req:Request){try{const body=await req.json();for(const k of ['firstName','lastName','jobTitle','organisation','email','country'])if(!body[k])return NextResponse.json({error:`Missing required field: ${k}`},{status:400});if(!/^\S+@\S+\.\S+$/.test(String(body.email)))return NextResponse.json({error:'Please enter a valid business email.'},{status:400});const payload={form_type:'speaker_interest',status:'NEW',submitted_at:new Date().toISOString(),...body};const hook=process.env.MAKE_SPEAKER_WEBHOOK_URL;if(hook){const r=await fetch(hook,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(payload)});if(!r.ok)throw new Error(`Make webhook returned ${r.status}`)}else console.info('Speaker interest captured; configure MAKE_SPEAKER_WEBHOOK_URL for Connectiva OS routing.',payload);return NextResponse.json({ok:true,message:'Your speaker interest has been received.'})}catch(e){console.error(e);return NextResponse.json({error:'We could not submit your interest. Please try again.'},{status:500})}}
